server:
  port: 8080

spring:
  application:
    name: user-auth-system
  
  # 数据库配置
  datasource:
    url: jdbc:mysql://101.200.126.122:3306/user_auth_system?useUnicode=true&characterEncoding=utf8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: Lgd4888983
  
  # JPA配置
  jpa:
    database-platform: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  
  # H2控制台配置
  h2:
    console:
      enabled: true
      path: /h2-console
  
  # Jackson配置
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
  
  # Redis配置
  redis:
    host: 101.200.126.122
    port: 6379
    password: 
    database: 0
    timeout: 2000ms
    lettuce:
      pool:
        max-active: 8
        max-wait: -1ms
        max-idle: 8
        min-idle: 0

# JWT配置
jwt:
  secret: your-secret-key-here-make-it-long-and-secure
  expiration: 86400000 # 24小时

# 日志配置
logging:
  level:
    com.example: DEBUG
    com.example.aspect: INFO  # API日志切面
    org.springframework.security: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"

# 微信公众号配置
wechat:
  token: 123456
  appid: wx848102408c0b8890
  appsecret: 321f129aebe2de6cdaf264fbd7bc5cb3
#  appid: wx0791373a537bc207
#  appsecret: bd3c235fc0fb0b608da47877ac4aa32b

sms:
  api-key: your_sms_api_key
  api-secret: your_sms_api_secret