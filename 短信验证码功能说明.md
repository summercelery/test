# 短信验证码功能说明

## 功能概述

本系统新增了短信验证码登录和忘记密码重置功能，提供更安全、便捷的用户认证方式。

## 主要功能

### 1. 短信验证码登录
- 用户可以通过手机号+验证码的方式登录
- 验证码6位数字，5分钟内有效
- 支持发送频率限制（60秒间隔，每日最多10次）

### 2. 忘记密码重置
- 通过手机号+验证码验证身份
- 设置新密码并确认
- 验证码类型与登录验证码分开管理

## 后端实现

### 新增DTO类

#### SmsLoginRequest.java
```java
- phoneNumber: 手机号（必填，格式验证）
- verificationCode: 验证码（必填，6位数字）
```

#### SendSmsRequest.java
```java
- phoneNumber: 手机号（必填，格式验证）
- smsType: 短信类型（LOGIN/RESET_PASSWORD）
```

#### ResetPasswordRequest.java
```java
- phoneNumber: 手机号（必填，格式验证）
- verificationCode: 验证码（必填，6位数字）
- newPassword: 新密码（必填，最少6位）
- confirmPassword: 确认密码（必填）
```

### 核心服务类

#### SmsService.java
主要功能：
- `sendVerificationCode()`: 发送验证码
- `verifyCode()`: 验证验证码
- `checkSendLimit()`: 检查发送限制
- `getRemainingSendCount()`: 获取剩余发送次数
- `getNextSendTime()`: 获取下次可发送时间

特性：
- 验证码5分钟有效期
- 发送间隔60秒限制
- 每日最多发送10次
- 验证码存储在Redis中

#### RedisService.java 扩展
新增方法：
- `setValue()`: 设置键值对（带过期时间）
- `getValue()`: 获取值
- `deleteKey()`: 删除键
- `hasKey()`: 检查键是否存在
- `getExpire()`: 获取剩余过期时间

### 认证服务扩展

#### AuthService.java 新增方法
- `sendSmsCode()`: 发送短信验证码
- `smsLogin()`: 短信验证码登录
- `resetPassword()`: 重置密码

### 控制器接口

#### AuthController.java 新增接口
- `POST /api/auth/send-sms`: 发送短信验证码
- `POST /api/auth/sms-login`: 短信验证码登录
- `POST /api/auth/reset-password`: 重置密码

## 前端实现

### 界面设计

#### 登录方式切换
- 密码登录（默认）
- 验证码登录
- 标签页式切换，支持动画效果

#### 短信登录表单
- 手机号输入框
- 验证码输入框（6位数字限制）
- 发送验证码按钮（带倒计时）

#### 忘记密码模态框
- 手机号输入
- 验证码输入和发送
- 新密码设置
- 确认密码验证

### JavaScript功能

#### 核心函数
- `switchLoginTab()`: 切换登录方式
- `sendSmsCode()`: 发送验证码（带倒计时）
- `handleSmsLogin()`: 处理短信登录
- `showForgotPasswordModal()`: 显示忘记密码模态框
- `handleResetPassword()`: 处理密码重置

#### 验证码倒计时
- 60秒倒计时显示
- 按钮状态管理
- 发送失败时重置状态

## 安全特性

### 1. 验证码安全
- 6位随机数字验证码
- 5分钟有效期
- 验证成功后立即删除
- 存储在Redis中，支持分布式部署

### 2. 发送限制
- 60秒发送间隔
- 每日最多10次发送
- 防止恶意刷验证码

### 3. 输入验证
- 手机号格式验证（11位数字）
- 验证码格式验证（6位数字）
- 密码强度要求（最少6位）

## 部署说明

### 1. 短信服务集成
当前使用模拟短信发送，实际部署时需要：
- 集成短信服务商API（如阿里云、腾讯云等）
- 配置短信模板
- 设置发送限制和频率控制

### 2. Redis配置
确保Redis服务正常运行：
```yaml
spring:
  redis:
    host: localhost
    port: 6379
    database: 0
```

### 3. 环境变量
建议配置以下环境变量：
- 短信服务商API密钥
- 短信模板ID
- Redis连接配置

## 测试用例

### 1. 短信登录测试
1. 切换到验证码登录
2. 输入手机号
3. 点击发送验证码
4. 输入收到的验证码
5. 点击登录

### 2. 忘记密码测试
1. 点击"忘记密码？"
2. 输入手机号
3. 发送验证码
4. 输入验证码和新密码
5. 确认重置

### 3. 限制测试
1. 连续发送验证码（应被限制）
2. 使用过期验证码（应验证失败）
3. 输入错误验证码（应提示错误）

## 注意事项

1. **短信服务**: 当前为模拟发送，实际部署需要集成真实短信服务
2. **验证码存储**: 使用Redis存储，确保Redis服务稳定
3. **频率限制**: 防止恶意刷验证码，保护短信服务成本
4. **用户体验**: 倒计时和状态提示，提升用户体验
5. **错误处理**: 完善的错误提示和异常处理

## 扩展功能

### 1. 图形验证码
可在发送短信前增加图形验证码验证

### 2. 语音验证码
支持语音验证码作为备选方案

### 3. 邮箱验证码
支持邮箱验证码登录

### 4. 多因素认证
结合密码和验证码的双重认证

### 5. 设备绑定
支持设备指纹识别和绑定

## 故障排除

### 1. 验证码发送失败
- 检查短信服务配置
- 验证Redis连接
- 查看发送限制设置

### 2. 验证码验证失败
- 检查验证码是否过期
- 确认Redis数据一致性
- 验证输入格式

### 3. 倒计时异常
- 检查JavaScript错误
- 验证DOM元素存在
- 确认事件绑定正确

## 性能优化

1. **Redis优化**: 使用Redis集群提高可用性
2. **缓存策略**: 合理设置验证码过期时间
3. **并发控制**: 防止验证码重复发送
4. **监控告警**: 监控短信发送成功率

## 总结

短信验证码功能为用户提供了更安全、便捷的登录方式，同时通过完善的安全机制和用户体验设计，确保了系统的安全性和易用性。在实际部署时，需要根据具体需求选择合适的短信服务商，并做好相关的配置和监控工作。 