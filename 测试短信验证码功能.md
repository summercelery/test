# 短信验证码功能测试指南

## 测试环境准备

### 1. 启动后端服务
```bash
# 确保Redis服务运行
redis-server

# 启动Spring Boot应用
mvn spring-boot:run
```

### 2. 启动前端服务
```bash
# 使用nginx或简单的HTTP服务器
# 例如使用Python的简单HTTP服务器
python -m http.server 80
```

## 功能测试

### 1. 短信验证码登录测试

#### 步骤1：打开前端页面
- 访问 http://localhost
- 点击"验证码登录"标签页

#### 步骤2：发送验证码
- 输入手机号：13800138000
- 点击"发送验证码"按钮
- 观察控制台输出（模拟短信发送）
- 验证码：6位随机数字

#### 步骤3：验证码登录
- 输入收到的验证码
- 点击"登录"按钮
- 验证登录成功

### 2. 忘记密码重置测试

#### 步骤1：打开忘记密码模态框
- 在密码登录页面点击"忘记密码？"
- 或直接访问忘记密码模态框

#### 步骤2：发送重置验证码
- 输入手机号：13800138000
- 点击"发送验证码"按钮
- 观察控制台输出

#### 步骤3：重置密码
- 输入收到的验证码
- 输入新密码：newpassword123
- 确认新密码：newpassword123
- 点击"重置密码"按钮
- 验证密码重置成功

### 3. 限制测试

#### 频率限制测试
- 连续点击"发送验证码"按钮
- 验证60秒间隔限制
- 验证倒计时功能

#### 验证码过期测试
- 发送验证码后等待5分钟
- 尝试使用过期验证码登录
- 验证过期提示

#### 错误验证码测试
- 输入错误的验证码
- 验证错误提示

## API测试

### 1. 发送短信验证码
```bash
curl -X POST http://localhost:8080/api/auth/send-sms \
  -H "Content-Type: application/json" \
  -d '{
    "phoneNumber": "13800138000",
    "smsType": "LOGIN"
  }'
```

### 2. 短信验证码登录
```bash
curl -X POST http://localhost:8080/api/auth/sms-login \
  -H "Content-Type: application/json" \
  -d '{
    "phoneNumber": "13800138000",
    "verificationCode": "123456"
  }'
```

### 3. 重置密码
```bash
curl -X POST http://localhost:8080/api/auth/reset-password \
  -H "Content-Type: application/json" \
  -d '{
    "phoneNumber": "13800138000",
    "verificationCode": "123456",
    "newPassword": "newpassword123",
    "confirmPassword": "newpassword123"
  }'
```

## 预期结果

### 1. 成功场景
- 验证码发送成功，控制台显示短信内容
- 登录成功，返回JWT token
- 密码重置成功，可以新密码登录

### 2. 错误场景
- 手机号格式错误：提示"手机号格式不正确"
- 验证码错误：提示"验证码错误或已过期"
- 用户不存在：提示"用户不存在，请先注册"
- 发送频率过高：提示"验证码发送失败，请稍后重试"

### 3. 限制验证
- 60秒内不能重复发送验证码
- 验证码5分钟后过期
- 每日最多发送10次验证码

## 调试信息

### 1. 后端日志
观察Spring Boot控制台输出：
- 验证码生成和发送日志
- Redis存储和验证日志
- 用户认证和会话管理日志

### 2. 前端控制台
观察浏览器开发者工具：
- 网络请求和响应
- JavaScript错误信息
- 验证码倒计时状态

### 3. Redis数据
检查Redis中的数据：
```bash
# 连接Redis
redis-cli

# 查看验证码数据
KEYS "sms:code:*"

# 查看发送限制数据
KEYS "sms:interval:*"
KEYS "sms:count:*"
```

## 常见问题

### 1. 验证码发送失败
- 检查Redis服务是否运行
- 验证手机号格式
- 查看发送限制设置

### 2. 验证码验证失败
- 确认验证码未过期
- 检查Redis数据一致性
- 验证输入格式

### 3. 前端显示异常
- 检查JavaScript错误
- 验证API接口响应
- 确认网络连接正常

## 性能测试

### 1. 并发测试
- 模拟多个用户同时发送验证码
- 验证Redis并发处理能力
- 检查系统响应时间

### 2. 压力测试
- 大量验证码发送请求
- 验证系统稳定性
- 监控内存和CPU使用

## 安全测试

### 1. 验证码安全性
- 验证码随机性测试
- 防止暴力破解
- 验证码有效期控制

### 2. 接口安全性
- 参数验证测试
- 频率限制测试
- 错误信息泄露测试

## 总结

通过以上测试，可以验证短信验证码功能的完整性和安全性。确保在实际部署前，所有功能都能正常工作，并且具备良好的用户体验和安全性。 